/**
 * Layout: Two-Column Compact — STUB.
 *
 * Visual goal (when implemented): denser layout where the results
 * are listed as dot-leader rows (`Test ........ Value Unit (Range) [H/L]`)
 * inside a two-column flow rather than a single wide table. Patient
 * block is a compact horizontal strip. Designed for screening clinics
 * that fit more on one page.
 *
 * This stub re-exports the corporate-clean layout so the layout
 * dispatch machinery sees a valid module under this key. The real
 * implementation will replace this file entirely.
 */
export * from './corporate-clean.js';
