/**
 * Layout: Branded Modern Card — STUB.
 *
 * Visual goal (when implemented): full-width banner across the top
 * in the lab's accent colour with white text and the lab monogram
 * large. A slim left sidebar (light gray background) carries the
 * patient meta vertically. Results render as a vertical stack of
 * "cards" — each test is its own bordered card with the value large
 * + unit + reference range + a coloured flag chip. Modern app-y
 * aesthetic used by new-age chains (e.g. HealthifyMe-style apps,
 * 1mg labs).
 *
 * This stub re-exports the corporate-clean layout so the layout
 * dispatch machinery sees a valid module under this key. The real
 * implementation will replace this file entirely.
 */
export * from './corporate-clean.js';
