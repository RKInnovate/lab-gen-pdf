/**
 * Layout: Letterhead Minimal — STUB.
 *
 * Visual goal (when implemented): bold script-style letterhead at
 * the top with the lab name in large display type, tagline in italic
 * underneath, address right-aligned in small grey text. Patient info
 * as a flat key:value list (one line per field, no table). Results
 * in a borderless arrangement with thin gridlines only between
 * groups. Footer with only page number and a small repeat of the lab
 * address.
 *
 * This stub re-exports the corporate-clean layout so the layout
 * dispatch machinery sees a valid module under this key. The real
 * implementation will replace this file entirely.
 */
export * from './corporate-clean.js';
